import React, { Fragment } from "react";
import classes from "./CSS/TableCompnent.module.css";

const TableComponent = () => {
  /*
  const { elem, index } = curElem;
  console.log(curElem);
  console.log(index);
*/
  return <Fragment></Fragment>;
};

export default TableComponent;
